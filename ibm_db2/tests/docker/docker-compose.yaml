version: '3'

services:

  ibm_db2:
    image: ibmcom/db2express-c:${DB2_VERSION}
    container_name: ibm_db2
    command: ["db2start"]
    ports:
      - "50000:50000"
    environment:
      - LICENSE=accept
      - DB2INST1_PASSWORD=hunter2
